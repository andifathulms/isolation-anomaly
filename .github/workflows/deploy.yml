name: Build and deploy

# Pack validation gates the deploy (PRD §11). The oracle harness never runs
# here: it needs real databases in containers, and its output is committed as
# fixtures which the tests below check the simulator against.

on:
  push:
    branches: [main]
  workflow_dispatch:

# Action majors are pinned to ones that run on Node 24. The Node 20 runtime is
# deprecated and currently only works because the runner forces a newer one.

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm packs:validate
      - run: pnpm typecheck
      - run: pnpm lint
      # The simulator against the recorded real databases, both directions of
      # every anomaly, and the conflict graph against brute-force search.
      - run: pnpm test:run

  build:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
